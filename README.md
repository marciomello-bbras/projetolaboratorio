# projetolaboratorio

Laboratório de criação de aplicativo de contas a pagar por IA.

## Executar a micro-API

Use Python 3.12+ e inicie a aplicação com:

```bash
py -3 -m uvicorn app.main:app --reload
```

As variáveis de ambiente esperadas estão em `.env.example`.
